# Copiar para env.local. O ab-stack/docker-compose sobrescreve DATABASE_URL, REDIS_HOST, REDIS_PORT e PORT.
ENVIRONMENT=development
OPENAI_API_KEY=
JWT_SECRET=change-me-for-local
JWT_ISSUER=dev-issuer
JWT_AUDIENCE=dev-audience
